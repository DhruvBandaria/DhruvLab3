<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cons_layout_dhruv_fragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DhruvFragment">

    <TextView
        android:id="@+id/name_and_studentId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="72dp"
        android:text="@string/name_studentid"
        android:textStyle="bold"
        android:textSize="18dp"/>
    <TextView
        android:id="@+id/brush_width_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/brush_width"
        android:layout_marginTop="110dp"
        android:layout_marginLeft="25dp"
        />

    <Spinner
        android:id="@+id/spinner"
        android:layout_below="@id/name_and_studentId"
        android:layout_toRightOf="@id/brush_width_text"
        android:layout_marginTop="15dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/brush_size_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/brush_color"
        android:layout_below="@id/brush_width_text"
        android:layout_marginLeft="25dp"
        android:layout_marginTop="15dp"
        />
    <Spinner
        android:id="@+id/spinner2"
        android:layout_below="@id/spinner"
        android:layout_toRightOf="@id/brush_size_text"
        android:layout_marginTop="15dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/deleteButton"
        android:layout_width="63dp"
        android:layout_height="65dp"
        android:layout_below="@id/name_and_studentId"
        android:layout_marginLeft="76dp"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@id/spinner"
        android:background="@drawable/ic_delete" />

</RelativeLayout>